сюда я буду записывать все, чтобы не забыть

1. create_LDAP_tokens почти дублирует функционал create_tokens
2. verify_token проверяет только не истек ли срок давности токена. не знаю, насколько это правильно, по моему внутрянку аля user.id он тоже должен смотреть
3. сделать или не сделать единую точку входа для авторизации локальной и ldap или так и оставить /auth/login и /auth/login/ldap
4. у нас оказывается никаким образом нет хранения внутренних номеров, ни в конфигах, ни в бд!

5. в instancesCRUD есть функция для создания ВМ без помощи промежуточных файлов. нужно ее допилить и полноценно переключиться на нее
6. реализовать переключение между режимами логирования (cdr в бд/csv)

7.0 слить бд asterisk_cdr в asterisk
7. сделать sip номера в бд(применить alembik миграции из офф репозитория), а не только в конфигах и добавить возможность переключаться
